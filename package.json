{
  "name": "aurora-pickem",
  "version": "1.0.0",
  "description": "Privacy-preserving Pick'em game with FHE",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.cjs --network sepolia",
    "create-series": "npx hardhat run scripts/create-series.cjs --network sepolia"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@fhevm/solidity": "^0.9.0",
    "@fhevm/hardhat-plugin": "0.3.0-0",
    "@fhevm/mock-utils": "0.3.0-0",
    "hardhat": "^2.19.0"
  }
}
